--- lua_source {{{
require("noice").setup({
  messages = {
    enabled = false,
  },
  lsp = {
    hover = {
      enabled = false,
    },
    signature = {
      enabled = false,
    },
  },
})
--- }}}
